# Scripts

Executable analyses will live here. Each script must declare inputs, outputs, units, random seeds, software dependencies, and the model or prediction it tests. Scripts must not depend on paths outside this repository; input locations should be configurable and raw data should remain immutable.

Planned modules: symbolic stability checks, continuation/boundary mapping, identifiability simulations, site-resolved influx estimation, and held-out model comparison.

