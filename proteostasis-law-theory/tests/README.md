# Tests

Planned checks include dimensional consistency, resource conservation, nonnegative-state invariance, equilibrium and Jacobian validation, limiting-case behavior, numerical continuation regression tests, synthetic-data parameter recovery, and schema/unit validation for empirical inputs.

Every quantitative claim should eventually have a test that fails when its assumptions, units, or expected qualitative behavior are violated.

