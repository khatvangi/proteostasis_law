# Proteostasis Law

## Active path

**`envelope-paper/`** — canonical since 2026-07-29.

- manuscript: [`envelope-paper/manuscript/MANUSCRIPT.md`](envelope-paper/manuscript/MANUSCRIPT.md)
- reproduction instructions: [`envelope-paper/README.md`](envelope-paper/README.md)

Extant translation operates inside a finite proteostasis envelope
(`B_total ≤ C_buffer`). A reduced dynamical model with saturable rescue and
superlinear aggregation feedback necessarily has a saddle-node collapse threshold;
two bounding arguments place E. coli roughly one order of magnitude inside that
envelope. Separately, the genetic code is strongly organized on the mistranslation
axis; on the translational-supply axis no structure is detectable, within a
stated power bound.

Not a code-origin claim.

Every number in the manuscript is regenerated by `envelope-paper/scripts/` from
`envelope-paper/data/raw/` and asserted by `envelope-paper/tests/` (73 tests).

## Superseded

| Path | Status |
|---|---|
| `combined-paper/` | superseded — see [`combined-paper/SUPERSEDED.md`](combined-paper/SUPERSEDED.md) for the four blocking problems |
| `proteostasis-paper/` | superseded — earlier evidence-ledger scaffold; its 15 tests check structure, not numbers |
| `manscuript-final.md` | stale root draft; N = 17,166 and several odds ratios are not reproducible from data on disk |
| `legacy_root_triplet_attempt/` | archive of the older triplet-origin / Pareto / metal-enrichment work; not active evidence |

## Two claims rejected on the evidence

Both appeared in `combined-paper/` and are excluded from the active manuscript.
The corrected analyses are reproduced by
`envelope-paper/scripts/07_removed_results.py` and reported in the manuscript's
Discussion rather than quietly dropped.

- **Metal-binding-site codon deployment.** A gene-level expression confound.
  Against non-metal positions of the same genes, 0 of 4 tests remain significant
  and His falls from OR 1.33 to OR 1.07. Roughly 40% of annotated sites are also
  mispositioned in sequence space.
- **Cross-species conservation of operational geometry.** Δ_A used E. coli μ for
  all three species, and 44 of 60 E. coli tAI values were bit-identical to the
  yeast values.

## Corrupt upstream data

`../codon-deployment/data/computed/{ecoli,bsubtilis,scerevisiae}_tai_ws.tsv` must
not be used as a supply (ν) axis; see
`../codon-deployment/data/computed/README_TAI_IS_CORRUPT.md`.
`../structural-criticality/` symlinks all three, so its tAI-axis results need
recomputation.
